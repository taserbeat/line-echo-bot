{
  "name": "line-echo-bot",
  "version": "1.0.0",
  "description": "LINE echo bot",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "git push heroku master",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  }
}
